# Set this in Netlify or your local .env.local file.
# The real value should be the Resend key you provided, but it should not be committed in source files.
RESEND_API_KEY=your_resend_api_key
TO_EMAIL=your_destination_email@example.com
FROM_EMAIL=your_verified_sender@example.com
